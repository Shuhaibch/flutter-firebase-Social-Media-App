name: post_repository
description: Dart packages for post 
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  uuid: ^4.1.0
  cloud_firestore: ^4.11.0
  flutter:
    sdk: flutter
  firebase_storage: ^11.4.0

  user_repository:
    path: ../user_repository
# Fire Base

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


  

flutter:
  uses-material-design: true
